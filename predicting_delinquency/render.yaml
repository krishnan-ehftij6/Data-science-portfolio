services:
  - type: web
    name: fastapi-service
    runtime: python
    buildCommand: ""
    startCommand: uvicorn predicting_delinquency.app.predict:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
    plan: free
